# Features

Feature-specific code goes here, one folder per domain feature (e.g. `booking/`, `services/`, `gallery/`). Each feature folder groups its own components, hooks, and types that aren't shared elsewhere.

Cross-feature, generic building blocks stay in `src/components`, `src/hooks`, `src/lib`, and `src/services`.
